# Fun City 服务器使用说明

## 当前推荐方式

项目已移除旧的 `.bat` 启动脚本。当前推荐用 Node 启动本地静态服务器：

```bash
node local-server.js
```

启动后访问：

```text
http://127.0.0.1:8080
```

停止服务器时，在启动服务器的终端里按 `Ctrl+C`。

## 为什么移除旧脚本

原来的批处理脚本依赖 `python -m http.server 8080`，但当前电脑执行 `python --version` 会报 Python 安装/系统配置错误。部分脚本还包含较复杂的中文、符号和 emoji 输出，在 `cmd` 下实测会出现解析错误。

## 常见问题

### 端口 8080 被占用

可以先查看占用进程：

```powershell
Get-NetTCPConnection -LocalPort 8080
```

如需停止对应进程：

```powershell
Stop-Process -Id <PID>
```

### 页面打不开

确认服务器终端显示：

```text
Fun City server running at http://127.0.0.1:8080
```

然后重新访问 `http://127.0.0.1:8080`。

### 游戏记录怎么清理

游戏记录保存在浏览器 LocalStorage 中。可以在浏览器开发者工具的 Application/Storage 面板里清理 `http://127.0.0.1:8080` 的 LocalStorage。

本项目使用的存储键（按前缀归类）：

| 前缀 / 键 | 用途 |
|---|---|
| `lg_players` / `lg_current_player` | 玩家档案列表与当前档案 |
| `lg_records_v1` | 排行榜（按玩家分组） |
| `lg_history_v1` | 测试/命理历史记录（按玩家分组） |
| `lg_achievements_v1` | 成就解锁记录（按玩家分组） |
| `lg_stats_v1` | 累计统计（按玩家分组） |
| `lg_audio_v1` | 音乐/音效开关偏好 |
| `schulte_bestms_*` | 舒尔特各模式最佳用时 |
| `schulte_poetry_stats_*` | 每首唐诗的完成度统计 |
| `sudoku_bestms_*` | 数独各难度最佳用时 |
| `memory_bestms_*` | 记忆翻牌各难度最佳记录 |
| `2048_best_score` | 2048 历史最高分 |
| `gomoku_best_win` | 五子棋最快获胜 |

只重置"平台功能"（玩家/排行榜/成就）时，删除 `lg_` 开头的键即可，游戏内最佳记录不受影响。
